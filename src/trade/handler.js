const { validationResult } = require( 'express-validator' );
const { Service: { TradeService } } = require( '../../lib' );

const get_trade_info = async function get_trade_info ( req, res ) {}

module.exports = {

    get_trade_info,

}